meta {
  name: Delete Product
  type: http
  seq: 5
}

delete {
  url: {{host}}/catalog/products/:productId
  body: none
  auth: none
}

params:path {
  productId: {{productId}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
