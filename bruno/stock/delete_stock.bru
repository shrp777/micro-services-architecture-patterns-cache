meta {
  name: Delete Stock
  type: http
  seq: 4
}

delete {
  url: {{stockHost}}/stocks/:productId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

params:path {
  productId: {{productId}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
